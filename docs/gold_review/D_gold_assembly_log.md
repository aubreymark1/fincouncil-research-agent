# GOLD-D-001 Gold 组装记录

> 角色：D
> 组装日期：2026-08-28
> 状态：进行中，等待输入合并与 A 裁决
> 正式评分门禁：保持 `pending_signoff`，A 签收前不得用于 E0—E3

## 一、输入快照

本轮只读核对以下云端角色分支，不把未合并内容伪装成主分支交付：

| 输入 | 提交 | 当前情况 |
|---|---|---|
| `origin/role-a-core` Gold 草案与任务板 | `235ffe2` | 两份草案各 5 项，但仍为 `pending_signoff` |
| `origin/role-b-ingestion` 来源核验 | `a36611d` | 10 项均给出 quote、页码、publisher、hash；新增两个食品安全政策来源 |
| `origin/role-c-industry` 口径复核 | `8f86cf8` | 口径复核已交付，同时记录 BLOCKER-1～4 和页码/逐字匹配问题 |
| 当前主分支基线 | `ac81016` | 尚未包含上述 A/B/C Gold 专项提交 |

当前本地 Gold fixture 继续保留空 `items`。这是显式门禁，不代表 B/C 没有工作成果；原因是 B/C 交付尚未合并，且多项内容尚未形成一致结论。

## 二、逐项组装判定

### 食品饮料

| 指标 | 已确认内容 | 未决问题 | D 判定 |
|---|---|---|---|
| `revenue_growth` | 数值 `-1.21%`，`DOC-FOOD-001`，B 核验物理页 6 | `expected_text` 需采用能由原文支持的表达 | 数据已具备，等待 A 确认文本形态 |
| `gross_margin` | B 核验酒类毛利率 `91.23%`，物理页 10 | A 草案为“茅台酒毛利率 93.53%”、页 18，C 也确认草案全文无命中 | 阻塞，不能沿用 A 草案 |
| `sales_expense_rate` | 分子 `7,253,499,600.68` 元、分母 `168,838,102,514.79` 元；复算 `4.296127%`，两位小数为 `4.30%` | 该值为派生计算；评分器要求 quote 与 fact_text 均逐字包含派生短语，与“quote 不得改写”冲突 | 阻塞，需 A 决定计算型指标表达或评分语义 |
| `inventory` | 原值 `61,427,421,796.18` 元，换算约 `614.27` 亿元 | B 记录页 14；A/C 草案或核验记录页 57；换算短语不是原文逐字文本 | 阻塞，需统一物理页码和计算表达 |
| `food_safety` | B 新增 `DOC-FOOD-012`、`DOC-FOOD-013` 两个政策来源；publisher 与 hash 均独立 | 新 PDF/manifest 尚未合并；A 草案仍引用旧公司年报来源；每个来源应保存各自原文，不能合并成斜杠短语 | 阻塞，等待 B 交付合并并由 A 确认来源级表达 |

### 银行

| 指标 | 已确认内容 | 未决问题 | D 判定 |
|---|---|---|---|
| `net_interest_margin` | 工商银行净利息收益率 `1.28%` | 表格提取文本与草案自然语言存在空格/逐字匹配差异 | 数值已具备，等待 A 确认文本形态 |
| `loan_growth` | 客户贷款及垫款增长 `7.5%` | A/C 指向页 34，B 主核验表指向页 21 并另提页 14 精确值 | 阻塞，需统一作为 Gold locator 的物理页 |
| `non_performing_loan_ratio` | 不良贷款率 `1.31%`，B 表格页 16 | 自然语言短语与表格提取形态不同 | 数据已具备，等待 A 确认文本形态 |
| `provision_coverage` | 拨备覆盖率 `213.60%` | A/B 使用页 16 表格，C 严格短语命中页 87 | 阻塞，需明确 locator 应指表格值还是严格短语页 |
| `capital_adequacy` | 资本充足率 `18.76%` | A 为页 102，B 为页 16，C 严格短语命中页 101；并表口径仍需确认 | 阻塞，需统一页码与监管口径 |

## 三、本轮 D 已完成

1. 固定 A/B/C 输入提交，避免复核时引用漂移。
2. 完成 10 个 required 指标的三方对账矩阵。
3. 明确记录计算型指标、multiple 来源、页码和逐字匹配阻塞，不通过改写 quote 或删除证据要求绕过。
4. 更新两份 Gold fixture 的门禁说明与配置来源基线，继续保持 `pending_signoff` 和空 `items`。
5. 增加测试，保证待签收 fixture：
   - 声明的 required metric 集合与行业配置完全一致；
   - 不包含会被误认为真实结果的 item；
   - 明确声明 A 签收前不得正式评分；
   - 仍会被正式 Gold loader 拒绝。

## 四、等待项与责任人

| 等待项 | 责任人 | 完成条件 | 解锁内容 |
|---|---|---|---|
| 合并 B 的来源核验、两个政策 PDF、manifest 和测试 | B/A | B 分支 PR 合并且 CI 通过 | `food_safety` 可做真实来源校验 |
| 合并 C 的口径复核与银行迁移产物 | C/A | C 分支 PR 合并且复核意见被接收 | D 可引用稳定的口径与迁移证据 |
| 裁决三方数值、页码和 expected_text 冲突 | A，B/C 复核 | 10 项均有唯一来源页、逐字文本或明确计算表达 | D 可填充两份 Gold 的 `items` |
| 解决计算型和来源级文本与评分器的兼容性 | A/D | 不降低证据标准，且不要求伪造 quote | Gold 可通过真实内容校验 |
| D 填充 JSON 并完成 PDF 页数、manifest、公式、multiple 独立性测试 | D | 上述输入稳定 | 可提交 A 最终签收 |
| Gold 最终签收 | A | B/C/D 交叉复核通过，状态改为 `signed` | 才能接入 E0—E3 正式评分 |

## 五、当前交付结论

本轮产物可以作为 `GOLD-D-001` 的预组装与阻塞清单提交审查，但不能标记任务完成。当前不生成、推测或补写正式实验结果，也不修改 `evaluation/experiment_definitions.yaml`。
